# Pinned to the exact set confirmed working via /healthz (2026-07-15). These
# define Garmin's login/token behavior AND its device/TLS fingerprint; letting
# any of them float meant a redeploy could pull a version that broke the saved
# tokens or tripped Garmin's bot-detection → forced re-login. Bump deliberately,
# one at a time, and re-test the Garmin login after any change.
#
# garminconnect 0.3.x dropped garth and drives auth via curl_cffi (TLS
# impersonation) + ua-generator (user-agent). curl_cffi/ua-generator drift can
# change the fingerprint Garmin sees, so they are pinned too.
garminconnect==0.3.6
curl_cffi==0.15.0
ua-generator==2.1.2
requests==2.34.2
flask>=3.0,<4
PyJWT[crypto]>=2.8,<3
